const value = Math.random() < 0.5 ? "a" : "b";
if (value !== "a") {
    console.log("a")
} else if (value === "b") {
    console.log("b")
}


// Go over tooling as well

// NoImpictAny - Javascript Any type

// strictNullChecks - Will alert you whenever it encounters a null type

let response = {
    contently: "Hello",
    responses: 200
}